{
  "name": "interimio-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper for the Interimio web app (iOS / Android)",
  "scripts": {
    "build:web": "cd ../frontend && yarn build",
    "sync": "npx cap sync",
    "ios": "npx cap open ios",
    "android": "npx cap open android",
    "release": "yarn build:web && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
